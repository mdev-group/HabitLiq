name: Debug File Structure

on: [push, workflow_dispatch]

jobs:
  debug:
    runs-on: macos-14
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: List LiquidGlassHabit Folder Contents
      run: |
        echo "=== What is inside LiquidGlassHabit ==="
        ls -R LiquidGlassHabit
